# Core app
streamlit>=1.35.0

# Deep learning — CPU-only torch for Streamlit Cloud compatibility
# (Streamlit Cloud runs on CPU; MPS/CUDA not available)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

transformers>=4.40.0
accelerate>=0.28.0

# Image + array handling
pillow>=10.0.0
numpy>=1.26.0
scikit-learn>=1.4.0

# YOLOv8 object detector
ultralytics>=8.0.0

# Report export
reportlab>=4.1.0
streamlit-image-coordinates>=0.1.7
